<Window x:Class="InterlinedList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:vm="clr-namespace:InterlinedList.ViewModels"
        Title="InterlinedList"
        Height="800" Width="1280"
        MinHeight="560" MinWidth="840"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource AppBgBrush}">

    <!-- WindowChrome: native resize + snap with a custom caption area -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="36"
                      CornerRadius="0"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="6"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>  <!-- Title bar -->
            <RowDefinition Height="*"/>   <!-- App body -->
        </Grid.RowDefinitions>

        <!-- ── Custom title bar ─────────────────────────────────────── -->
        <Grid Grid.Row="0" Background="{StaticResource TitlebarBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo + app name (drag region) -->
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="12,0,0,0">
                <Image Source="../brand-kit/logo/logo-icon.svg"
                       Width="18" Height="18"
                       Margin="0,0,8,0"
                       VerticalAlignment="Center"/>
                <TextBlock Text="InterlinedList"
                           Foreground="{StaticResource TitlebarTextBrush}"
                           FontSize="12"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Window controls: Min / Max / Close -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        WindowChrome.IsHitTestVisibleInChrome="True">

                <Button x:Name="BtnMinimize"
                        ToolTip="Minimize"
                        Click="BtnMinimize_Click"
                        Style="{StaticResource TitlebarBtnStyle}">
                    <Path Data="M0,5 H10" Stroke="White" StrokeThickness="1.2"
                          Width="10" Height="10" Stretch="None"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>

                <Button x:Name="BtnMaximize"
                        ToolTip="Maximize"
                        Click="BtnMaximize_Click"
                        Style="{StaticResource TitlebarBtnStyle}">
                    <Rectangle Width="9" Height="9"
                               Stroke="White" StrokeThickness="1.2"
                               Fill="Transparent"/>
                </Button>

                <Button x:Name="BtnClose"
                        ToolTip="Close"
                        Click="BtnClose_Click"
                        Style="{StaticResource TitlebarCloseBtnStyle}">
                    <Path Data="M1,1 L9,9 M9,1 L1,9"
                          Stroke="White" StrokeThickness="1.2"
                          Width="10" Height="10" Stretch="None"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>

            </StackPanel>
        </Grid>

        <!-- ── App body ──────────────────────────────────────────────── -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>  <!-- Left nav -->
                <ColumnDefinition Width="*"/>    <!-- Feed -->
                <ColumnDefinition Width="280"/>  <!-- Right rail -->
            </Grid.ColumnDefinitions>

            <!-- ── Left navigation ───────────────────────────────────── -->
            <Grid Grid.Column="0" Background="{DynamicResource MastheadBrush}">
                <StackPanel Margin="0,8,0,0">

                    <Button x:Name="NavFeed"    Tag="Feed"          Click="NavItem_Click"
                            Style="{StaticResource NavItemStyle}" Content="Feed"/>
                    <Button x:Name="NavLists"   Tag="Lists"         Click="NavItem_Click"
                            Style="{StaticResource NavItemStyle}" Content="Lists"/>
                    <Button x:Name="NavDocuments" Tag="Documents"   Click="NavItem_Click"
                            Style="{StaticResource NavItemStyle}" Content="Documents"/>
                    <Button x:Name="NavOrganizations" Tag="Organizations" Click="NavItem_Click"
                            Style="{StaticResource NavItemStyle}" Content="Organizations"/>
                    <Button x:Name="NavSearch"  Tag="Search"        Click="NavItem_Click"
                            Style="{StaticResource NavItemStyle}" Content="Search"/>
                    <Button x:Name="NavAccounts" Tag="Accounts"     Click="NavItem_Click"
                            Style="{StaticResource NavItemStyle}" Content="Accounts"/>
                    <Button x:Name="NavAlerts"  Tag="Alerts"        Click="NavItem_Click"
                            Style="{StaticResource NavItemStyle}" Content="Alerts"/>

                </StackPanel>
            </Grid>

            <!-- ── Center content: swapped by the left nav ──────────── -->
            <ContentControl x:Name="CenterContent" Grid.Column="1"/>

            <!-- ── Right rail ─────────────────────────────────────────── -->
            <Border Grid.Column="2"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="16">

                        <!-- Stream clock -->
                        <Border Background="{StaticResource TealBrush}"
                                CornerRadius="10"
                                Padding="16,12"
                                Margin="0,0,0,20">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="STREAM CLOCK"
                                           FontFamily="JetBrains Mono, Consolas"
                                           FontSize="9" FontWeight="Medium"
                                           Foreground="#99FFFFFF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBlock x:Name="ClockDisplay"
                                           FontFamily="JetBrains Mono, Consolas"
                                           FontSize="22" FontWeight="Medium"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           Text="--:--:--Z"/>
                            </StackPanel>
                        </Border>

                        <!-- Profile summary (default rail content) -->
                        <StackPanel x:Name="ProfileRail" Visibility="Visible">
                            <Border Width="56" Height="56"
                                    CornerRadius="999"
                                    ClipToBounds="True"
                                    Background="{DynamicResource Surface3Brush}"
                                    HorizontalAlignment="Center"
                                    Margin="0,0,0,10">
                                <Image Source="{Binding Profile.Avatar}" Stretch="UniformToFill"/>
                            </Border>
                            <TextBlock Text="{Binding Profile.DisplayName}"
                                       FontSize="14" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock FontSize="12"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       HorizontalAlignment="Center"
                                       Margin="0,2,0,8">
                                <Run Text="@"/><Run Text="{Binding Profile.Username}"/>
                            </TextBlock>
                            <TextBlock Text="{Binding Profile.Bio}"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextBodyBrush}"
                                       TextWrapping="Wrap"
                                       TextAlignment="Center"
                                       Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                                <TextBlock Margin="0,0,16,0">
                                    <Run Text="{Binding Profile.Followers}" FontWeight="SemiBold"/>
                                    <Run Text=" followers" Foreground="{DynamicResource TextMutedBrush}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="{Binding Profile.Following}" FontWeight="SemiBold"/>
                                    <Run Text=" following" Foreground="{DynamicResource TextMutedBrush}"/>
                                </TextBlock>
                            </StackPanel>
                            <Button Content="Log out"
                                    Command="{Binding Profile.LogoutCommand}"
                                    HorizontalAlignment="Center"
                                    Style="{StaticResource DigBtnStyle}"/>
                        </StackPanel>

                        <!-- Notifications (shown via the Alerts nav item) -->
                        <StackPanel x:Name="NotificationsRail" Visibility="Collapsed">
                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="ALERTS"
                                           FontSize="10" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           VerticalAlignment="Center"/>
                                <Button Content="Mark all read"
                                        Command="{Binding Notifications.MarkAllReadCommand}"
                                        HorizontalAlignment="Right"
                                        Style="{StaticResource DigBtnStyle}"/>
                            </Grid>

                            <ItemsControl ItemsSource="{Binding Notifications.Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type vm:NotificationItemViewModel}">
                                        <Border Margin="0,0,0,8"
                                                Background="{DynamicResource Surface2Brush}"
                                                BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                Padding="10,8">
                                            <StackPanel>
                                                <DockPanel Margin="0,0,0,4">
                                                    <Ellipse DockPanel.Dock="Left"
                                                             Width="6" Height="6"
                                                             Fill="{StaticResource AmberBrush}"
                                                             Margin="0,0,6,0"
                                                             VerticalAlignment="Center"
                                                             Visibility="{Binding IsUnread, Converter={StaticResource BoolToVisibility}}"/>
                                                    <TextBlock Text="{Binding TimeFormatted}"
                                                               DockPanel.Dock="Right"
                                                               FontFamily="JetBrains Mono, Consolas"
                                                               FontSize="9"
                                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                                    <TextBlock Text="{Binding Title}"
                                                               FontSize="11" FontWeight="SemiBold"
                                                               Foreground="{DynamicResource TextBrush}"
                                                               TextWrapping="Wrap"/>
                                                </DockPanel>
                                                <TextBlock Text="{Binding Body}"
                                                           FontSize="11"
                                                           Foreground="{DynamicResource TextMutedBrush}"
                                                           TextWrapping="Wrap"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>
            </Border>

        </Grid><!-- /.app-body Grid -->
    </Grid><!-- /root Grid -->

    <!-- ── Styles ────────────────────────────────────────────────── -->
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <!-- Title bar button base -->
        <Style x:Key="TitlebarBtnStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1EFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#14FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close button (red hover) -->
        <Style x:Key="TitlebarCloseBtnStyle" TargetType="Button"
               BasedOn="{StaticResource TitlebarBtnStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFE81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Nav item -->
        <Style x:Key="NavItemStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource NavTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource GreenBrush}"
                                BorderThickness="3,0,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                            </Border.Style>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{DynamicResource NavHoverBgBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{DynamicResource NavActiveBgBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary green button -->
        <Style x:Key="PrimaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{DynamicResource PrimaryHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Amber Dig button -->
        <Style x:Key="DigBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AmberBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AmberBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1AF0A830"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

</Window>
