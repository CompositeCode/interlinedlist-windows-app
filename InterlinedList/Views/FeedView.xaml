<UserControl x:Class="InterlinedList.Views.FeedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:InterlinedList.ViewModels"
             xmlns:local="clr-namespace:InterlinedList.Views"
             Background="{DynamicResource AppBgBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibility"/>

        <!-- Primary green button -->
        <Style x:Key="PrimaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{DynamicResource PrimaryHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Amber Dig button -->
        <Style x:Key="DigBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AmberBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AmberBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1AF0A830"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Subtle card action (Reply / Edit / Delete / Report) -->
        <Style x:Key="CardActionBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource Surface3Brush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tag chip: clickable, filters the feed by that tag -->
        <Style x:Key="TagChipStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Surface3Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource LinkBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,4,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background"
                                        Value="{DynamicResource BorderStrongBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Clickable author name → opens their profile -->
        <Style x:Key="AuthorLinkBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Compose -->
            <RowDefinition Height="Auto"/>  <!-- Error banner -->
            <RowDefinition Height="Auto"/>  <!-- Tag filter + trending -->
            <RowDefinition Height="*"/>     <!-- Post list -->
        </Grid.RowDefinitions>

        <!-- Feed header -->
        <Border Grid.Row="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,12">
            <Grid>
                <TextBlock Text="Feed"
                           FontSize="17" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}"
                           VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Loading…"
                               FontSize="12"
                               Foreground="{DynamicResource TextMutedBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"
                               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"/>
                    <Button Content="{Binding ScheduledToggleLabel}"
                            Command="{Binding ToggleScheduledCommand}"
                            Style="{StaticResource CardActionBtnStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Compose -->
        <Border Grid.Row="1"
                Background="{DynamicResource Surface2Brush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,12">
            <StackPanel>
                <TextBox Text="{Binding ComposeText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="52"
                         MaxHeight="120"
                         Padding="10,8"
                         FontSize="13"
                         Background="{DynamicResource SurfaceBrush}"
                         Foreground="{DynamicResource TextBodyBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         BorderThickness="1"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <CheckBox Content="Bluesky"
                              IsChecked="{Binding CrossPostToBluesky}"
                              Visibility="{Binding IsBlueskyLinked, Converter={StaticResource BoolToVisibility}}"
                              Foreground="{DynamicResource TextMutedBrush}"
                              FontSize="12"
                              Margin="0,0,12,0"
                              VerticalAlignment="Center"/>
                    <CheckBox Content="Twitter"
                              IsChecked="{Binding CrossPostToTwitter}"
                              Visibility="{Binding IsTwitterLinked, Converter={StaticResource BoolToVisibility}}"
                              Foreground="{DynamicResource TextMutedBrush}"
                              FontSize="12"
                              Margin="0,0,12,0"
                              VerticalAlignment="Center"/>
                    <CheckBox Content="Mastodon"
                              IsChecked="{Binding CrossPostToMastodon}"
                              Visibility="{Binding MastodonProvider, Converter={StaticResource NullOrEmptyToVisibility}}"
                              Foreground="{DynamicResource TextMutedBrush}"
                              FontSize="12"
                              VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Tags for the next post -->
                <StackPanel Margin="0,8,0,0">
                    <!-- Already-added chips -->
                    <ItemsControl ItemsSource="{Binding ComposeTags}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Surface3Brush}"
                                        CornerRadius="3" Padding="8,2" Margin="0,0,4,4">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding StringFormat=#{0}}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextBodyBrush}"
                                                   VerticalAlignment="Center"/>
                                        <Button Content="✕"
                                                Command="{Binding DataContext.RemoveTagCommand,
                                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource CardActionBtnStyle}"
                                                Padding="4,0" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <DockPanel LastChildFill="True">
                        <Button DockPanel.Dock="Right"
                                Content="Add tag"
                                Command="{Binding AddTagCommand}"
                                Style="{StaticResource DigBtnStyle}"
                                Margin="6,0,0,0"/>
                        <TextBox Text="{Binding TagInput, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="8,4" FontSize="12"
                                 Background="{DynamicResource SurfaceBrush}"
                                 Foreground="{DynamicResource TextBodyBrush}"
                                 BorderBrush="{DynamicResource BorderBrush}"
                                 BorderThickness="1"
                                 ToolTip="Tags can contain spaces. Separate several with commas.">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Return" Command="{Binding AddTagCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </DockPanel>

                    <!-- Prefix autocomplete (GET /api/tags/autocomplete) -->
                    <ItemsControl ItemsSource="{Binding TagSuggestions}" Margin="0,6,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Label}"
                                        Command="{Binding DataContext.UseTagSuggestionCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding Tag}"
                                        Style="{StaticResource TagChipStyle}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <!-- Attached images (uploaded, pending post) -->
                <ItemsControl ItemsSource="{Binding AttachedImageUrls}" Margin="0,8,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,6,6" Width="52" Height="52" CornerRadius="4"
                                    ClipToBounds="True" Background="{DynamicResource Surface3Brush}">
                                <Grid>
                                    <Image Source="{Binding}" Stretch="UniformToFill"/>
                                    <Button Content="✕"
                                            Command="{Binding DataContext.RemoveAttachmentCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Right" VerticalAlignment="Top"
                                            Width="16" Height="16" Padding="0" FontSize="9"
                                            Background="#99000000" Foreground="White"
                                            BorderThickness="0" Cursor="Hand"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Attached videos (uploaded, pending post) -->
                <ItemsControl ItemsSource="{Binding AttachedVideoUrls}" Margin="0,6,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,4" Background="{DynamicResource Surface3Brush}"
                                    CornerRadius="3" Padding="8,4">
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Content="✕"
                                            Command="{Binding DataContext.RemoveVideoAttachmentCommand,
                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource CardActionBtnStyle}"/>
                                    <TextBlock Text="🎬 video attached" FontSize="11"
                                               Foreground="{DynamicResource TextBodyBrush}" VerticalAlignment="Center"/>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Schedule -->
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                    <CheckBox Content="Schedule"
                              IsChecked="{Binding IsScheduling}"
                              Foreground="{DynamicResource TextMutedBrush}"
                              FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker SelectedDate="{Binding ScheduleDate}" Width="130"
                                VerticalAlignment="Center" Margin="0,0,8,0"
                                Visibility="{Binding IsScheduling, Converter={StaticResource BoolToVisibility}}"/>
                    <TextBox Text="{Binding ScheduleTime, UpdateSourceTrigger=PropertyChanged}"
                             Width="56" VerticalAlignment="Center" Padding="6,3"
                             Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource TextBodyBrush}"
                             BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                             Visibility="{Binding IsScheduling, Converter={StaticResource BoolToVisibility}}"/>
                    <TextBlock Text="local HH:mm" FontSize="10" VerticalAlignment="Center" Margin="6,0,0,0"
                               Foreground="{DynamicResource TextMutedBrush}"
                               Visibility="{Binding IsScheduling, Converter={StaticResource BoolToVisibility}}"/>
                </StackPanel>

                <DockPanel Margin="0,8,0,0" LastChildFill="False">
                    <Button DockPanel.Dock="Left"
                            Content="Add image"
                            Command="{Binding AttachImageCommand}"
                            Style="{StaticResource DigBtnStyle}"/>
                    <Button DockPanel.Dock="Left"
                            Content="Add video"
                            Command="{Binding AttachVideoCommand}"
                            Style="{StaticResource DigBtnStyle}"
                            Margin="6,0,0,0"/>
                    <Button DockPanel.Dock="Right"
                            Content="Post"
                            Command="{Binding PostCommand}"
                            Style="{StaticResource PrimaryBtnStyle}"/>
                </DockPanel>
            </StackPanel>
        </Border>

        <!-- Error banner -->
        <Border Grid.Row="2"
                Background="#1AE81123"
                Padding="20,8">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding ErrorMessage}"
                       FontSize="12"
                       Foreground="#FFE81123"
                       TextWrapping="Wrap"/>
        </Border>

        <!--
            Tag filter + trending. Lives inside FeedView rather than the shell's
            right rail: MainWindow.xaml is in open PR #149, and the feed is the
            only place tags mean anything anyway.
        -->
        <Border Grid.Row="3"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,8">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding HasActiveTag}" Value="False"/>
                                <Condition Binding="{Binding HasTrendingTags}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel>
                <!-- Which tag the feed is filtered to -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6"
                            Visibility="{Binding HasActiveTag, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="{Binding ActiveTagLabel}"
                               FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}"
                               VerticalAlignment="Center"/>
                    <Button Content="✕ Clear"
                            Command="{Binding ClearTagFilterCommand}"
                            Style="{StaticResource CardActionBtnStyle}"
                            Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Trending (GET /api/tags/trending) -->
                <StackPanel Visibility="{Binding HasTrendingTags, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="Trending"
                               FontSize="10" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextMutedBrush}"
                               Margin="0,0,0,4"/>
                    <ItemsControl ItemsSource="{Binding TrendingTags}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Label}"
                                        Command="{Binding DataContext.FilterByTagCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding Tag}"
                                        Style="{StaticResource TagChipStyle}"
                                        ToolTip="{Binding CountLabel}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Post list -->
        <ScrollViewer Grid.Row="4"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="16,12">
            <StackPanel>
              <!-- Normal feed (hidden while viewing scheduled) -->
              <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowScheduled}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <ItemsControl x:Name="FeedList"
                              ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:MessageItemViewModel}">
                            <Border Margin="0,0,0,8"
                                    Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Left edge: amber once you've Dug it, teal otherwise -->
                                    <Border Grid.Column="0" CornerRadius="4,0,0,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource TealBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DugByMe}" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource AmberBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>

                                    <!-- Card content -->
                                    <StackPanel Grid.Column="1" Margin="12,10">

                                        <!-- "↻ Pushed" / "❝ Quoted" badge -->
                                        <TextBlock Text="{Binding RepostLabel}"
                                                   FontSize="10" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TealAccentBrush}"
                                                   Margin="0,0,0,4"
                                                   Visibility="{Binding IsPushOrQuote, Converter={StaticResource BoolToVisibility}}"/>

                                        <!-- Header row -->
                                        <DockPanel Margin="0,0,0,6">
                                            <Border DockPanel.Dock="Left"
                                                    Width="24" Height="24"
                                                    CornerRadius="999"
                                                    ClipToBounds="True"
                                                    Background="{DynamicResource Surface3Brush}"
                                                    Margin="0,0,8,0">
                                                <Image Source="{Binding AvatarUrl}" Stretch="UniformToFill"/>
                                            </Border>
                                            <Button DockPanel.Dock="Left"
                                                    Content="{Binding AuthorDisplayName}"
                                                    Command="{Binding OpenAuthorCommand}"
                                                    Style="{StaticResource AuthorLinkBtnStyle}"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,6,0"/>
                                            <TextBlock DockPanel.Dock="Left"
                                                       Text="{Binding AuthorHandle}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                            <TextBlock DockPanel.Dock="Right"
                                                       Text="{Binding TimeFormatted}"
                                                       FontFamily="JetBrains Mono, Consolas"
                                                       FontSize="10"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       HorizontalAlignment="Right"/>
                                        </DockPanel>

                                        <!-- Body. MentionText renders @mentions as
                                             clickable links (#94); the Visibility
                                             binding below collapses this for a bare
                                             Push, which carries no commentary (#31).
                                             Both belong on this one element. -->
                                        <TextBlock local:MentionText.Text="{Binding Content}"
                                                   FontSize="13"
                                                   Foreground="{DynamicResource TextBodyBrush}"
                                                   TextWrapping="Wrap"
                                                   LineHeight="20"
                                                   Margin="0,0,0,8"
                                                   Visibility="{Binding HasContent, Converter={StaticResource BoolToVisibility}}"/>

                                        <!-- Embedded original, from the nested pushedMessage -->
                                        <Border Margin="0,0,0,8"
                                                Background="{DynamicResource Surface2Brush}"
                                                BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1" CornerRadius="3" Padding="10,8"
                                                Visibility="{Binding HasQuotedOriginal, Converter={StaticResource BoolToVisibility}}">
                                            <StackPanel>
                                                <DockPanel Margin="0,0,0,4">
                                                    <Border DockPanel.Dock="Left"
                                                            Width="18" Height="18" CornerRadius="999"
                                                            ClipToBounds="True"
                                                            Background="{DynamicResource Surface3Brush}"
                                                            Margin="0,0,6,0">
                                                        <Image Source="{Binding QuotedAvatarUrl}" Stretch="UniformToFill"/>
                                                    </Border>
                                                    <Button DockPanel.Dock="Left"
                                                            Content="{Binding QuotedAuthorDisplayName}"
                                                            Command="{Binding OpenQuotedAuthorCommand}"
                                                            Style="{StaticResource AuthorLinkBtnStyle}"
                                                            FontSize="11"
                                                            VerticalAlignment="Center"
                                                            Margin="0,0,6,0"/>
                                                    <TextBlock DockPanel.Dock="Left"
                                                               Text="{Binding QuotedAuthorHandle}"
                                                               FontSize="10"
                                                               Foreground="{DynamicResource TextMutedBrush}"
                                                               VerticalAlignment="Center"/>
                                                    <TextBlock DockPanel.Dock="Right"
                                                               Text="{Binding QuotedTimeFormatted}"
                                                               FontFamily="JetBrains Mono, Consolas"
                                                               FontSize="10"
                                                               Foreground="{DynamicResource TextMutedBrush}"
                                                               HorizontalAlignment="Right"/>
                                                </DockPanel>
                                                <TextBlock Text="{Binding QuotedContent}"
                                                           FontSize="12"
                                                           Foreground="{DynamicResource TextBodyBrush}"
                                                           TextWrapping="Wrap" LineHeight="18"
                                                           MaxHeight="120" TextTrimming="CharacterEllipsis"/>
                                                <ItemsControl ItemsSource="{Binding QuotedImageUrls}" Margin="0,6,0,0"
                                                              Visibility="{Binding QuotedHasImages, Converter={StaticResource BoolToVisibility}}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Margin="0,0,0,4" CornerRadius="3"
                                                                    ClipToBounds="True" HorizontalAlignment="Left">
                                                                <Image Source="{Binding}" Stretch="Uniform" MaxHeight="180"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>

                                        <!-- Attached images -->
                                        <ItemsControl ItemsSource="{Binding ImageUrls}" Margin="0,0,0,8"
                                                      Visibility="{Binding HasImages, Converter={StaticResource BoolToVisibility}}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Margin="0,0,0,6" CornerRadius="4"
                                                            ClipToBounds="True" HorizontalAlignment="Left">
                                                        <Image Source="{Binding}" Stretch="Uniform" MaxHeight="320"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Attached videos (open in browser) -->
                                        <ItemsControl ItemsSource="{Binding VideoUrls}" Margin="0,0,0,8"
                                                      Visibility="{Binding HasVideos, Converter={StaticResource BoolToVisibility}}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="🎬 Play video"
                                                            Command="{Binding DataContext.OpenVideoCommand,
                                                                      RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource CardActionBtnStyle}"
                                                            HorizontalAlignment="Left"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Tag chips → filter the feed by that tag -->
                                        <ItemsControl ItemsSource="{Binding Tags}" Margin="0,0,0,6"
                                                      Visibility="{Binding HasTags, Converter={StaticResource BoolToVisibility}}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Command="{Binding DataContext.FilterByTagCommand,
                                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource TagChipStyle}">
                                                        <TextBlock Text="{Binding StringFormat=#{0}}"/>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Footer: Dig + actions (wraps — the row is long on a narrow window) -->
                                        <WrapPanel>
                                            <Button Content="Dig"
                                                    Command="{Binding DigCommand}">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource DigBtnStyle}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding DugByMe}" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource AmberBrush}"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                            <TextBlock Text="{Binding DigCount}"
                                                       FontFamily="JetBrains Mono, Consolas"
                                                       FontSize="10"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="8,0,12,0"/>
                                            <!-- pushCount, alongside digCount -->
                                            <TextBlock Text="{Binding PushCount, StringFormat=↻ {0}}"
                                                       FontFamily="JetBrains Mono, Consolas"
                                                       FontSize="10"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,12,0"
                                                       ToolTip="Pushes"/>
                                            <Button Content="Push" Command="{Binding PushCommand}"
                                                    Style="{StaticResource CardActionBtnStyle}"
                                                    ToolTip="Re-share as-is. Always public."/>
                                            <Button Content="Quote" Command="{Binding StartQuoteCommand}"
                                                    Style="{StaticResource CardActionBtnStyle}"
                                                    ToolTip="Re-share with your own note. Always public."/>
                                            <Button Content="Reply" Command="{Binding StartReplyCommand}"
                                                    Style="{StaticResource CardActionBtnStyle}"/>
                                            <Button Content="Replies" Command="{Binding ToggleRepliesCommand}"
                                                    Style="{StaticResource CardActionBtnStyle}"/>
                                            <Button Content="Edit" Command="{Binding StartEditCommand}"
                                                    Style="{StaticResource CardActionBtnStyle}"
                                                    Visibility="{Binding IsMine, Converter={StaticResource BoolToVisibility}}"/>
                                            <Button Content="Delete" Command="{Binding DeleteCommand}"
                                                    Style="{StaticResource CardActionBtnStyle}"
                                                    Visibility="{Binding IsMine, Converter={StaticResource BoolToVisibility}}"/>
                                            <Button Content="Report" Command="{Binding ReportCommand}"
                                                    Style="{StaticResource CardActionBtnStyle}"
                                                    Visibility="{Binding CanReport, Converter={StaticResource BoolToVisibility}}"/>
                                        </WrapPanel>

                                        <!-- Per-card status (a failed Push/Quote, or "Reported. Thanks…") -->
                                        <TextBlock Text="{Binding ErrorMessage}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource AmberBrush}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,6,0,0"
                                                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyToVisibility}}"/>

                                        <!-- Quote composer: the banner IS the "this will be public" confirmation -->
                                        <StackPanel Margin="0,8,0,0"
                                                    Visibility="{Binding IsComposingQuote, Converter={StaticResource BoolToVisibility}}">
                                            <Border Background="#1AF0A830"
                                                    BorderBrush="{StaticResource AmberBrush}"
                                                    BorderThickness="1" CornerRadius="3"
                                                    Padding="8,6" Margin="0,0,0,6">
                                                <TextBlock Text="Quotes are always public — everyone will be able to see this, whatever your default visibility is."
                                                           FontSize="11"
                                                           Foreground="{DynamicResource TextBodyBrush}"
                                                           TextWrapping="Wrap"/>
                                            </Border>
                                            <TextBox Text="{Binding QuoteText, UpdateSourceTrigger=PropertyChanged}"
                                                     AcceptsReturn="True" TextWrapping="Wrap"
                                                     MinHeight="44" Padding="8,6" FontSize="13"
                                                     Background="{DynamicResource Surface2Brush}"
                                                     Foreground="{DynamicResource TextBodyBrush}"
                                                     BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                                                <Button Content="Cancel" Command="{Binding CancelQuoteCommand}"
                                                        Style="{StaticResource CardActionBtnStyle}" Margin="0,0,6,0"/>
                                                <Button Content="Quote publicly" Command="{Binding PostQuoteCommand}"
                                                        Style="{StaticResource PrimaryBtnStyle}"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Inline edit -->
                                        <StackPanel Margin="0,8,0,0"
                                                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}">
                                            <TextBox Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}"
                                                     AcceptsReturn="True" TextWrapping="Wrap"
                                                     MinHeight="44" Padding="8,6" FontSize="13"
                                                     Background="{DynamicResource Surface2Brush}"
                                                     Foreground="{DynamicResource TextBodyBrush}"
                                                     BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                                                <Button Content="Cancel" Command="{Binding CancelEditCommand}"
                                                        Style="{StaticResource CardActionBtnStyle}" Margin="0,0,6,0"/>
                                                <Button Content="Save" Command="{Binding SaveEditCommand}"
                                                        Style="{StaticResource PrimaryBtnStyle}"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Reply composer -->
                                        <StackPanel Margin="0,8,0,0"
                                                    Visibility="{Binding IsComposingReply, Converter={StaticResource BoolToVisibility}}">
                                            <TextBox Text="{Binding ReplyText, UpdateSourceTrigger=PropertyChanged}"
                                                     AcceptsReturn="True" TextWrapping="Wrap"
                                                     MinHeight="44" Padding="8,6" FontSize="13"
                                                     Background="{DynamicResource Surface2Brush}"
                                                     Foreground="{DynamicResource TextBodyBrush}"
                                                     BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
                                                <Button Content="Cancel" Command="{Binding CancelReplyCommand}"
                                                        Style="{StaticResource CardActionBtnStyle}" Margin="0,0,6,0"/>
                                                <Button Content="Reply" Command="{Binding PostReplyCommand}"
                                                        Style="{StaticResource PrimaryBtnStyle}"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Replies -->
                                        <ItemsControl ItemsSource="{Binding Replies}" Margin="12,8,0,0"
                                                      Visibility="{Binding AreRepliesVisible, Converter={StaticResource BoolToVisibility}}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type vm:MessageItemViewModel}">
                                                    <Border Margin="0,0,0,6"
                                                            Background="{DynamicResource Surface2Brush}"
                                                            BorderBrush="{DynamicResource BorderBrush}"
                                                            BorderThickness="1" CornerRadius="3" Padding="10,8">
                                                        <StackPanel>
                                                            <DockPanel Margin="0,0,0,4">
                                                                <TextBlock DockPanel.Dock="Left" Text="{Binding AuthorDisplayName}"
                                                                           FontSize="11" FontWeight="SemiBold"
                                                                           Foreground="{DynamicResource TextBrush}" Margin="0,0,6,0"/>
                                                                <TextBlock DockPanel.Dock="Left" Text="{Binding AuthorHandle}"
                                                                           FontSize="10" Foreground="{DynamicResource TextMutedBrush}"/>
                                                                <TextBlock DockPanel.Dock="Right" Text="{Binding TimeFormatted}"
                                                                           FontFamily="JetBrains Mono, Consolas" FontSize="10"
                                                                           Foreground="{DynamicResource TextMutedBrush}"
                                                                           HorizontalAlignment="Right"/>
                                                            </DockPanel>
                                                            <TextBlock local:MentionText.Text="{Binding Content}" FontSize="12"
                                                                       Foreground="{DynamicResource TextBodyBrush}"
                                                                       TextWrapping="Wrap" LineHeight="18"/>
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content="Load more"
                        Command="{Binding LoadMoreCommand}"
                        HorizontalAlignment="Center"
                        Margin="0,4,0,16"
                        Style="{StaticResource DigBtnStyle}"
                        Visibility="{Binding HasMore, Converter={StaticResource BoolToVisibility}}"/>
              </StackPanel>

              <!-- Scheduled posts (shown via the header toggle) -->
              <StackPanel Visibility="{Binding ShowScheduled, Converter={StaticResource BoolToVisibility}}">
                  <TextBlock Text="Scheduled posts"
                             FontSize="13" FontWeight="SemiBold"
                             Foreground="{DynamicResource TextBrush}"
                             Margin="0,0,0,8"/>
                  <ItemsControl ItemsSource="{Binding ScheduledMessages}">
                      <ItemsControl.ItemTemplate>
                          <DataTemplate DataType="{x:Type vm:MessageItemViewModel}">
                              <Border Margin="0,0,0,8"
                                      Background="{DynamicResource SurfaceBrush}"
                                      BorderBrush="{DynamicResource BorderBrush}"
                                      BorderThickness="1" CornerRadius="4" Padding="12,10">
                                  <StackPanel>
                                      <TextBlock Text="⏱ Scheduled" FontSize="10" FontWeight="SemiBold"
                                                 Foreground="{StaticResource AmberBrush}" Margin="0,0,0,4"/>
                                      <TextBlock local:MentionText.Text="{Binding Content}" FontSize="13"
                                                 Foreground="{DynamicResource TextBodyBrush}" TextWrapping="Wrap"/>
                                  </StackPanel>
                              </Border>
                          </DataTemplate>
                      </ItemsControl.ItemTemplate>
                  </ItemsControl>
              </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
