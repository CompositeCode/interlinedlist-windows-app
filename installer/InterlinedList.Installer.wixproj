<Project Sdk="WixToolset.Sdk/5.0.2">

  <PropertyGroup>
    <Configuration Condition="'$(Configuration)' == ''">Release</Configuration>
    <Platform Condition="'$(Platform)' == ''">x64</Platform>
    <OutputType>Package</OutputType>
    <OutputName>InterlinedList-Setup</OutputName>

    <!-- Published output of the WPF app that this MSI packages. Produced by the
         PublishApp target below before the MSI is linked. -->
    <AppPublishDir>$(MSBuildThisFileDirectory)..\InterlinedList\bin\$(Configuration)\net10.0-windows\win-x64\publish</AppPublishDir>
    <DefineConstants>PublishDir=$(AppPublishDir)</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="WixToolset.UI.wixext" Version="5.0.2" />
  </ItemGroup>

  <!-- Publishing is a prerequisite this project does NOT perform itself: WiX's
       <Files Include> harvesting runs before any BeforeTargets="Build" hook gets
       a chance to run (confirmed empirically in CI — a same-file auto-publish
       target here silently never fired), so `dotnet publish` on the app project
       must be run as a separate, earlier step. See CLAUDE.md / the CI workflow. -->

</Project>
